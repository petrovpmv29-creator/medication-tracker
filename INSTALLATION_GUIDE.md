# 📱 Инструкция по установке приложения "Дневник таблеток"

## Важные файлы:
Для работы приложения нужны ВСЕ эти файлы в одной папке:
- medication-tracker-mobile.html (основной файл)
- manifest.json (описание приложения)
- sw.js (для работы офлайн)
- icon-192.png (иконка малая)
- icon-512.png (иконка большая)

## 🍎 Установка на iPhone (iOS)

### Способ 1: Через Safari
1. Откройте файл medication-tracker-mobile.html в браузере Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой вверх) внизу экрана
3. Прокрутите вниз и выберите "На экран «Домой»"
4. Нажмите "Добавить" в правом верхнем углу
5. Готово! Иконка появится на домашнем экране

### Примечание для iOS:
- iOS не поддерживает автоматическую установку PWA
- Нужно вручную добавить через Safari
- После добавления приложение будет работать в полноэкранном режиме

## 🤖 Установка на Android

### Способ 1: Автоматическое уведомление
1. Откройте файл medication-tracker-mobile.html в браузере Chrome
2. Появится всплывающее уведомление "Установить приложение"
3. Нажмите "Установить"
4. Готово! Иконка появится на домашнем экране

### Способ 2: Через меню Chrome
1. Откройте файл medication-tracker-mobile.html в Chrome
2. Нажмите на три точки (⋮) в правом верхнем углу
3. Выберите "Добавить на главный экран" или "Установить приложение"
4. Нажмите "Установить"
5. Готово!

## 💻 Установка на компьютер (Windows/Mac/Linux)

### Google Chrome / Microsoft Edge:
1. Откройте medication-tracker-mobile.html в браузере
2. В адресной строке справа появится иконка установки (⊕ или значок компьютера)
3. Нажмите на неё
4. Нажмите "Установить"
5. Приложение откроется в отдельном окне

### Firefox:
Firefox не поддерживает установку PWA, но приложение будет работать в браузере.

## 🌐 Способ через веб-сервер (рекомендуется)

Для лучшей работы PWA рекомендуется разместить файлы на веб-сервере:

### Простой способ (Python):
1. Поместите все файлы в одну папку
2. Откройте терминал/командную строку в этой папке
3. Запустите команду:
   ```
   python -m http.server 8000
   ```
   или для Python 2:
   ```
   python -m SimpleHTTPServer 8000
   ```
4. Откройте браузер и перейдите по адресу: http://localhost:8000/medication-tracker-mobile.html
5. Теперь появится возможность установки как PWA

### Используя Node.js:
```
npx http-server
```

## ⚠️ Важные замечания:

1. **Все файлы должны быть в одной папке!**
   Если файлы разбросаны, иконки и офлайн-режим не будут работать.

2. **HTTPS или localhost**
   Service Worker работает только на HTTPS или localhost.
   Для локального использования это не проблема.

3. **Офлайн-режим**
   После первого запуска приложение будет работать даже без интернета.

4. **Данные сохраняются локально**
   Все ваши записи хранятся только на вашем устройстве.

## 🔧 Проверка установки

После установки проверьте:
- ✅ Приложение открывается в полноэкранном режиме (без адресной строки браузера)
- ✅ На домашнем экране есть иконка с градиентом и символом таблетки
- ✅ Приложение работает без интернета после первого запуска
- ✅ Данные сохраняются после закрытия приложения

## 🆘 Решение проблем

**Не появляется уведомление об установке:**
- Убедитесь, что все файлы в одной папке
- Попробуйте открыть через локальный сервер (см. выше)
- Очистите кэш браузера и попробуйте снова

**Не работают иконки:**
- Проверьте, что файлы icon-192.png и icon-512.png находятся в той же папке

**Не работает офлайн-режим:**
- Service Worker требует HTTPS или localhost
- Попробуйте запустить через локальный веб-сервер

**На iOS не появляется иконка установки:**
- Это нормально для iOS - используйте ручной способ через Safari

## 📞 Дополнительная информация

Приложение использует технологию PWA (Progressive Web App):
- Работает на всех платформах
- Не требует магазина приложений
- Обновляется автоматически
- Использует минимум памяти

Все данные хранятся локально в браузере с помощью localStorage.
